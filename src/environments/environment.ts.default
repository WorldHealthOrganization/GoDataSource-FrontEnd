export const environment = {
    // enable Production mode
    production: false,
    // API base URL
    apiUrl: 'http://whoapi3.clarisoft.com/api',
    // log messages on server?
    enableApiLogging: false,
    // log messages in browser's console?
    enableClientLogging: true
};
